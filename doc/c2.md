# C2 : Estrutura do Frontend 
Este capítulo documenta a organização do código React, os **padrões utilizados bem como componentes reutilizados** no código.



## Componentes reutilizáveis de UI

| Componente | Ficheiro | Uso |
|------------|----------|-----|
| `FilterBar` | `components/common/FilterBar.jsx` | Container flex para filtros |
| `FilterInput` | `components/common/FilterInput.jsx` | Campo de pesquisa (admin, clientes) |
| `FilterSelect` | `components/common/FilterSelect.jsx` | Select estilizado; prop `sort` alinha à direita |
| `ActiveFiltersBar` | `components/common/ActiveFiltersBar.jsx` | Tags de filtros activos com limpar |
| `PageTopbar` | `components/common/PageTopbar.jsx` | Título + acções; prop `section` para sub-secções |
| `EmptyState` | `components/common/EmptyState.jsx` | Mensagem quando não há dados |
| `PageModal` | `components/common/PageModal.jsx` | Overlay + cabeçalho de modal CRUD |
| `FormFooter` | `components/common/FormFooter.jsx` | Botões Cancelar / Guardar dentro do modal |
| `ClientTipoSection` | `components/common/ClientTipoSection.jsx` | Secção Profissionais / Pessoais (cliente) |
| `ListItemActions` | `components/common/ListItemActions.jsx` | Botões Editar / Apagar nos `*Item` |

*Ref. -`src/components/common/FilterBar.jsx`, linhas 7-10:*

```7:10:src/components/common/FilterBar.jsx
const FilterBar = ({ children, className }) => (
  <div className={`pg-filter-bar${className ? ` ${className}` : ''}`}>
    {children}
  </div>
);
```

Constantes e utilitários partilhados:

| Módulo | Ficheiro | Conteúdo |
|--------|----------|----------|
| Filtros | `constants/filters.js` | Grupos musculares, estados de sessão, ordenação |
| Planos | `constants/planos.js` | `OBJETIVOS_PLANO`, `labelObjetivoPlano()` |
| Datas | `utils/formatDate.js` | `formatDateShortPt`, `formatDateLongPt`, `formatDateTimePt` |
| Utilizadores | `utils/userDisplay.js` | `nomeUtilizador()`, `encontrarUtilizador()` |

*Ref. -`src/constants/planos.js`, linhas 1-13:*

```1:13:src/constants/planos.js
/** Objetivos de plano - valor API + label PT */
export const OBJETIVOS_PLANO = [
  { value: 'emagrecimento', label: 'Emagrecimento' },
  { value: 'hipertrofia',   label: 'Hipertrofia'   },
  { value: 'resistencia',   label: 'Resistência'   },
  { value: 'flexibilidade', label: 'Flexibilidade' },
  { value: 'saude_geral',   label: 'Saúde Geral'   },
];

export function labelObjetivoPlano(valor) {
  const item = OBJETIVOS_PLANO.find((o) => o.value === valor);
  return item ? item.label : valor || '-';
}
```

---

## Padrões React 

### 1. Entrada da aplicação

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Montar a SPA no DOM com `ReactDOM.createRoot` | `src/index.js` | `14-21` |
| Importar folhas de estilo globais | `src/index.js` | `5-12` |

*Ref. -`src/index.js`, linhas 5-12 e 14-21:*

```5:21:src/index.js
import './styles/index.css';
import './styles/common.css';
import './styles/Navbar.css';
import './styles/LoginPage.css';
import './styles/ClientDashboard.css';
import './styles/PlanoCard.css';
import './styles/components.css';
import './pages/pages.css';

// React 18 - ReactDOM.createRoot obrigatório
const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
```

### 2. Create React App e proxy de desenvolvimento

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Scripts `start` / `build` com `react-scripts` e porta 8080 | `package.json` | `14-18` |
| Template HTML com `<div id="root">` | `public/index.html` | `11` |
| Reencaminhar pedidos `/auth`, `/planos`, etc. para a API em dev | `src/setupProxy.js` | `12-21` |
| Build estático + nginx em produção | `Dockerfile` | `1-18` |
| Dev server com hot reload | `Dockerfile.dev` | `1-20` |

*Ref. -`src/setupProxy.js`, linhas 12-21:*

```12:21:src/setupProxy.js
module.exports = function (app) {
  app.use(
    createProxyMiddleware(
      ['/auth', '/planos', '/exercicios', '/sessoes', '/users', '/avaliacoes', '/metas'],
      {
        target,
        changeOrigin: true,
      }
    )
  );
};
```

### 3. Class components e `state`

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Declarar state inicial no `constructor` | `src/pages/LoginPage.jsx` | `12-19` |
| Atualizar state com `setState` | `src/pages/LoginPage.jsx` | `25-27` |
| State de tabs e filtros no dashboard | `src/pages/trainer/TrainerDashboard.jsx` | `25-31` |
| State de listagem, filtros e modal CRUD | `src/pages/trainer/PlanosPage.jsx` | `39-57` |

*Ref. -`src/pages/trainer/PlanosPage.jsx`, linhas 39-57:*

```39:57:src/pages/trainer/PlanosPage.jsx
    this.state = {
      planos:       [],
      clientes:     [],
      treinadores:  [],
      loading:      true,
      erro:         null,
      // filtros
      filtroObjetivo: '',
      filtroCliente:  props.filtroClienteInicial  ? String(props.filtroClienteInicial)  : '',
      filtroTreinador: props.filtroTreinadorInicial ? String(props.filtroTreinadorInicial) : '',
      filtroAtivo:    '',
      filtroTipo:     '',
      // modal
      modalAberto:  false,
      planoAEditar: null,
      form:         { ...FORM_INICIAL },
      formErro:     null,
      formLoading:  false,
    };
```

### 4. Formulários controlados

O valor dos inputs vive no `state`; `onChange` atualiza o state; `value` lê do state.

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Handler genérico `handleChange` no login | `src/pages/LoginPage.jsx` | `25-27` |
| Inputs ligados ao state (`value` + `onChange`) | `src/pages/LoginPage.jsx` | `82-109` |
| Handler do formulário do modal CRUD | `src/pages/trainer/PlanosPage.jsx` | `144-151` |
| Submit com validação e `POST/PUT` à API | `src/pages/trainer/PlanosPage.jsx` | `158-208` |

*Ref. -`src/pages/trainer/PlanosPage.jsx`, linhas 144-151 e 186-202:*

```144:151:src/pages/trainer/PlanosPage.jsx
  handleFormChange(e) {
    const { name, value, type, checked } = e.target;
    const val = type === 'checkbox' ? checked : value;
    this.setState((prev) => ({
      form:     { ...prev.form, [name]: val },
      formErro: null,
    }));
  }
```

```186:202:src/pages/trainer/PlanosPage.jsx
    try {
      if (planoAEditar) {
        await apiClient.put(`/planos/${planoAEditar.id}`, payloadBase);
      } else {
        const payload = {
          ...payloadBase,
          clienteId: Number(form.clienteId),
          tipo: (isAdmin && !form.treinadorId) ? 'pessoal' : 'profissional',
        };
        if (isAdmin && form.treinadorId) {
          payload.treinadorId = Number(form.treinadorId);
        }
        await apiClient.post('/planos', payload);
      }
      this.fecharModal();
      this.carregarDados();
```

### 5. Lifting state up

O **pai** detém estado partilhado (tab ativa, filtros, plano selecionado) e passa-o aos **filhos** via props e callbacks.

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| State de navegação no pai (treinador) | `src/pages/trainer/TrainerDashboard.jsx` | `25-31` |
| Passar filtro e callback ao filho | `src/pages/trainer/TrainerDashboard.jsx` | `140-145` |
| Callback `onVerExercicios` muda tab e plano | `src/pages/trainer/TrainerDashboard.jsx` | `73-75` |
| State de tab e `filtroPlanoId` no pai (cliente) | `src/pages/client/ClientDashboard.jsx` | `37-41` |
| Reagir a filtro recebido do pai | `src/pages/trainer/PlanosPage.jsx` | `75-91` |

*Ref. -`src/pages/trainer/TrainerDashboard.jsx`, linhas 73-75 e 140-145:*

```73:75:src/pages/trainer/TrainerDashboard.jsx
  verExerciciosPlano(plano) {
    this.setState({ planoSelecionado: plano, tabAtiva: 'exercicios' });
  }
```

```140:145:src/pages/trainer/TrainerDashboard.jsx
            {tabAtiva === 'planos' && (
              <PlanosPage
                trainerId={trainerId}
                onVerExercicios={this.verExerciciosPlano}
                filtroClienteInicial={filtroClienteId}
              />
```

### 6. Lifecycle - carregar dados da API

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Carregar perfil ao montar dashboard | `src/pages/trainer/TrainerDashboard.jsx` | `41-51` |
| Carregar planos e clientes ao montar página | `src/pages/trainer/PlanosPage.jsx` | `71-73, 93-110` |
| Processar query params OAuth ao montar | `src/pages/OAuthCallbackPage.jsx` | `19-44` |
| Sincronizar props do pai após mount | `src/pages/trainer/PlanosPage.jsx` | `75-91` |

*Ref. -`src/pages/OAuthCallbackPage.jsx`, linhas 19-44:*

```19:44:src/pages/OAuthCallbackPage.jsx
  componentDidMount() {
    this.processarCallback();
  }

  processarCallback() {
    const params = new URLSearchParams(this.props.location.search);

    const apiKey = params.get('apiKey');
    const role   = params.get('role');
    const userId = params.get('userId');

    if (!apiKey || !role) {
      this.setState({
        erro: 'Autenticação OAuth falhou: parâmetros em falta na resposta do servidor.',
      });
      return;
    }

    localStorage.setItem('gymApiKey', apiKey);
    localStorage.setItem('gymUserRole', role);
    if (userId) {
      localStorage.setItem('gymUserId', userId);
    }

    this.setState({ pronto: true, role });
  }
```

### 7. Componentes funcionais e props

Componentes de apresentação recebem dados e callbacks via **props**, sem state próprio.

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Card reutilizável com callbacks `onEditar`, `onApagar`, `onVerExercicios` | `src/components/planos/PlanoCard.jsx` | `20-30` |
| Navbar com username, role e logout | `src/components/common/Navbar.jsx` | `21-38` |
| `<select>` controlado para filtros | `src/components/common/FilterSelect.jsx` | `10-22` |
| Modal CRUD reutilizável | `src/components/common/PageModal.jsx` | `7-31` |
| Cabeçalho de página | `src/components/common/PageTopbar.jsx` | `7-28` |
| Indicador de carregamento | `src/components/common/LoadingSpinner.jsx` | *(funcional)* |

*Ref. -`src/components/planos/PlanoCard.jsx`, linhas 20-30:*

```20:30:src/components/planos/PlanoCard.jsx
const PlanoCard = ({ plano, nomeCliente, onEditar, onApagar, onVerExercicios }) => {
  const objLabel = OBJETIVO_META[plano.objetivo] || plano.objetivo;
  const temAcoes = onEditar || onApagar;

  const subtexto = nomeCliente
    ? nomeCliente
    : (plano.treinador ? (plano.treinador.displayName || plano.treinador.username) : null);

  const handleCardClick = () => {
    if (onVerExercicios) onVerExercicios(plano.id);
  };
```

### 8. PropTypes

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Validar props do dashboard | `src/pages/trainer/TrainerDashboard.jsx` | `181-185` |
| Validar props da rota protegida | `src/routes/RoleRoute.jsx` | `42-45` |
| Validar props do callback OAuth | `src/pages/OAuthCallbackPage.jsx` | `82-86` |

*Ref. -`src/routes/RoleRoute.jsx`, linhas 42-45:*

```42:45:src/routes/RoleRoute.jsx
RoleRoute.propTypes = {
  component: PropTypes.elementType.isRequired,
  allowedRoles: PropTypes.arrayOf(PropTypes.string).isRequired,
};
```

### 9. Rotas e navegação

| Finalidade | Ficheiro | Linhas |
|------------|----------|--------|
| Rotas públicas (`/login`, `/oauth/callback`) | `src/App.jsx` | `37-40` |
| Rotas protegidas por role | `src/App.jsx` | `43-57` |
| Redirecionamento na raiz `/` por role | `src/App.jsx` | `59-69` |
| Guard de autenticação + role | `src/routes/RoleRoute.jsx` | `23-39` |

*Ref. -`src/routes/RoleRoute.jsx`, linhas 23-39:*

```23:39:src/routes/RoleRoute.jsx
const RoleRoute = ({ component: Component, allowedRoles, ...rest }) => (
  <Route
    {...rest}
    render={(props) => {
      if (!AuthService.isLoggedIn()) {
        return <Redirect to="/login" />;
      }

      const role = AuthService.getUserRole();

      if (!allowedRoles.includes(role)) {
        return <Redirect to="/unauthorized" />;
      }

      return <Component {...props} />;
    }}
  />
);
```

---

## Inventário de ficheiros

### Configuração e entrada

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `package.json` | Config | Dependências e scripts CRA |
| `public/index.html` | HTML | Template da SPA |
| `src/index.js` | Entrada | Mount React no DOM |
| `src/setupProxy.js` | Config | Proxy dev para a API |
| `Dockerfile` | Docker | Build CRA + nginx (produção) |
| `Dockerfile.dev` | Docker | CRA dev server com hot reload |

### Raiz e autenticação

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `src/App.jsx` | Class | Router e rotas da aplicação |
| `src/auth/AuthService.js` | Módulo JS | Login, logout, `localStorage` |
| `src/api/apiClient.js` | Módulo JS | Axios + interceptors |
| `src/routes/RoleRoute.jsx` | Funcional | Proteção de rotas por role |

### Páginas públicas

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `src/pages/LoginPage.jsx` | Class | Formulário de login + OAuth |
| `src/pages/OAuthCallbackPage.jsx` | Class | Processar retorno OAuth |
| `src/pages/UnauthorizedPage.jsx` | Funcional | Acesso negado por role |

### Dashboards

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `src/pages/admin/AdminDashboard.jsx` | Class | Gestão de utilizadores; reutiliza páginas do treinador |
| `src/pages/trainer/TrainerDashboard.jsx` | Class | Tabs, filtros, navegação entre recursos |
| `src/pages/client/ClientDashboard.jsx` | Class | Tabs e filtros do cliente |

### Páginas do treinador (CRUD)

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `src/pages/trainer/ClientesPage.jsx` | Class | Lista clientes |
| `src/pages/trainer/PlanosPage.jsx` | Class | CRUD planos + filtros + modal |
| `src/pages/trainer/ExerciciosPage.jsx` | Class | CRUD exercícios por plano |
| `src/pages/trainer/SessoesPage.jsx` | Class | CRUD sessões |
| `src/pages/trainer/AvaliacoesPage.jsx` | Class | CRUD avaliações |
| `src/pages/trainer/MetasPage.jsx` | Class | CRUD metas |

### Páginas do cliente

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `src/pages/client/ClientPlanosPage.jsx` | Class | Lista planos do cliente |
| `src/pages/client/ClientExerciciosPage.jsx` | Class | Exercícios filtrados por plano |
| `src/pages/client/ClientSessoesPage.jsx` | Class | Sessões do cliente |
| `src/pages/client/ClientAvaliacoesPage.jsx` | Class | Avaliações + formulário pessoal |
| `src/pages/client/ClientMetasPage.jsx` | Class | Metas do cliente |

### Componentes de apresentação

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `src/components/planos/PlanoCard.jsx` | Funcional | Card de plano |
| `src/components/metas/MetaItem.jsx` | Funcional | Item de meta |
| `src/components/trainer/ExercicioItem.jsx` | Funcional | Item de exercício |
| `src/components/trainer/SessaoItem.jsx` | Funcional | Item de sessão |
| `src/components/trainer/AvaliacaoItem.jsx` | Funcional | Item de avaliação |
| `src/components/trainer/ClienteCard.jsx` | Funcional | Card de cliente do ginásio |
| `src/components/trainer/ClienteDetailPanel.jsx` | Class | Drawer com detalhe do cliente |
| `src/components/admin/UserRow.jsx` | Funcional | Linha na tabela admin |
| `src/components/common/Navbar.jsx` | Funcional | Barra de navegação |
| `src/components/common/LoadingSpinner.jsx` | Funcional | Indicador de carregamento |
| `src/components/common/ErrorMessage.jsx` | Funcional | Mensagem de erro |
| `src/components/common/FilterBar.jsx` | Funcional | Container de filtros |
| `src/components/common/FilterInput.jsx` | Funcional | Campo de pesquisa |
| `src/components/common/FilterSelect.jsx` | Funcional | Select controlado |
| `src/components/common/ActiveFiltersBar.jsx` | Funcional | Filtros ativos visíveis |
| `src/components/common/PageTopbar.jsx` | Funcional | Cabeçalho de página |
| `src/components/common/EmptyState.jsx` | Funcional | Estado vazio |
| `src/components/common/PageModal.jsx` | Funcional | Modal CRUD |
| `src/components/common/FormFooter.jsx` | Funcional | Rodapé de formulário modal |
| `src/components/common/ClientTipoSection.jsx` | Funcional | Secção Profissionais/Pessoais |
| `src/components/common/ListItemActions.jsx` | Funcional | Botões Editar/Apagar |

### Utilitários e estilos

| Ficheiro | Tipo | Finalidade |
|----------|------|------------|
| `src/constants/filters.js` | Módulo JS | Funções puras de filtro/ordenação |
| `src/constants/planos.js` | Módulo JS | Objetivos de plano partilhados |
| `src/utils/formatDate.js` | Módulo JS | Formatação de datas pt-PT |
| `src/utils/userDisplay.js` | Módulo JS | Nomes de utilizadores |
| `src/styles/*.css` | CSS | Estilos globais, login, dashboard |
| `src/pages/pages.css` | CSS | Estilos partilhados CRUD (`pg-*`) |

---

## Recursos da API consumidos pelo frontend

| Recurso | Endpoints usados | Páginas |
|---------|------------------|---------|
| Auth | `POST /auth/login`, `/auth/github`, `/auth/google` | `LoginPage`, `OAuthCallbackPage` |
| Utilizadores | `GET /users`, `GET /users/me`, `PUT /users/:id/role`, `DELETE /users/:id` | `AdminDashboard`, dashboards |
| Planos | `GET/POST/PUT/DELETE /planos` | `PlanosPage`, `ClientPlanosPage` |
| Exercícios | `GET/POST/PUT/DELETE /exercicios` | `ExerciciosPage`, `ClientExerciciosPage` |
| Sessões | `GET/POST/PUT/DELETE /sessoes` | `SessoesPage`, `ClientSessoesPage` |
| Avaliações | `GET/POST/PUT/DELETE /avaliacoes` | `AvaliacoesPage`, `ClientAvaliacoesPage` |
| Metas | `GET/POST/PUT/DELETE /metas` | `MetasPage`, `ClientMetasPage` |

*Ref. -`src/api/apiClient.js`, linhas 22-32 (interceptor que injeta `X-API-Key` em todos os pedidos):*

```22:32:src/api/apiClient.js
// --- Interceptor de REQUEST ---
apiClient.interceptors.request.use(
  (config) => {
    const apiKey = AuthService.getApiKey();
    if (apiKey) {
      config.headers['X-API-Key'] = apiKey;
    }
    return config;
  },
  (error) => Promise.reject(error)
);
```

---

[< Anterior](c1.md) | [^ Início](../README.md) | [Próximo >](c3.md)
:--- | :---: | ---:
