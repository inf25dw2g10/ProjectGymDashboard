# C1 : Apresentação do Projeto

## Descrição

Este trabalho tem como objetivo desenvolver uma camada de serviços *REST*, protegida por uma camada de Autenticação e Autorização, para disponibilizar recursos a uma aplicação *web*.

A plataforma permite que treinadores e clientes criem e façam a gestão de planos de treino, definam os exercícios incluídos em cada plano e registem as sessões de treino realizadas.

## Base de dados

A base de dados integra as seguintes tabelas: 

- Utilizadores
- Planos de Treino
- Exercícios
- Sessões
- Avaliações Físicas
- Metas

## Relações Implementadas

### Relações 1:N (Um para Muitos)

- **Utilizadores → Planos de Treino**: Um treinador cria vários planos; um cliente tem vários planos atribuídos
- **Planos de Treino → Exercícios**: Um plano tem múltiplos exercícios; um exercício pertence a um plano
- **Planos de Treino → Sessões**: Um plano tem multiplas sessões registadas
- **Utilizadores → Sessões**: Um cliente realiza várias sessões; um treinador supervisiona várias sessões
- **Utilizadores → Avaliações Físicas**: Um cliente tem várias avaliações físicas
- **Utilizadores → Metas**: Um cliente tem muitas metas definidas
- **Planos de Treino → Metas**: Um plano pode ter várias metas associadas

## Arquitetura

<img src="/doc/images/Arquitetura_GymAPI.jpg" alt="Arquitetura do Projeto" width="500">  



## Instalação

### Pré-requisitos:
- *Docker* instalado
- Portas 3000 e 3307 disponíveis 

**Configurar variáveis de ambiente:**

Criar o ficheiro `.env` na raiz do projeto:

```env
# .env.example 
SESSION_SECRET=uma_string_super_secreta

GITHUB_CLIENT_ID=**todo**
GITHUB_CLIENT_SECRET=**todo**

GOOGLE_CLIENT_ID=**todo**
GOOGLE_CLIENT_SECRET=**todo**
```

**Criar as *OAuth Apps*:**  
- *GitHub* em [github.com/settings/developers](https://github.com/settings/developers), *callback URL*: `http://localhost:3000/auth/github/callback`
- *Google* em [console.cloud.google.com](https://console.cloud.google.com), *callback URL*: `http://localhost:3000/auth/google/callback`
   

### Instruções de instalação:

1. Iniciar os *containers*:
   ```bash
   # docker-compose.dev.yml
   docker compose -f docker-compose.dev.yml up --build

   # docker-compose.prod.yml
   docker compose -f docker-compose.prod.yml up 
   ```

2. *migrations*/*seeders*:
   - `db:migrate` e `db:seed:all` são executados no arranque do container da *API*.
   - Se for preciso correr manualmente:  
     
   ```bash
   docker exec gym_api_dev npx sequelize-cli db:migrate
   docker exec gym_api_dev npx sequelize-cli db:seed:all
   ```

3. Aguardar até que todos os *containers* estejam em execução. A *API* escreve no terminal quando estiver pronta:
   ```
   ╔══════════════════════════════════════════╗
   ║  Project Gym API - porta 3000            ║
   ║  Docs : http://localhost:3000/docs       ║
   ║  Login: http://localhost:3000/login      ║
   ╚══════════════════════════════════════════╝
   ```

## Uso

Após instalação e execução do *compose*, a *API* fica disponível em:

- [*localhost*:3000/docs](http://localhost:3000/docs)  
  Interface de documentação com *Swagger UI*. As respostas são devolvidas em *JSON*.

- [*localhost*:3000/login](http://localhost:3000/login)  
  Página de autenticação. Suporta *OAuth2* (*GitHub* e *Google*) e *Basic Auth*.


## Endpoints disponíveis

| Método | Endpoint | Descrição | Role mínima |
|---|---|---|---|
| GET | `/login` | Página de *login* | Público |
| GET | `/auth/github` | Iniciar *OAuth2* (*GitHub*) | Público |
| GET | `/auth/github/callback` | *Callback OAuth2* (*GitHub*) | Público |
| GET | `/auth/google` | Iniciar *OAuth2* (*Google*) | Público |
| GET | `/auth/google/callback` | *Callback OAuth2* (*Google*) | Público |
| POST | `/auth/register` | Registar com *Basic Auth* | Público |
| POST | `/auth/login` | *Login* com *username*/email + *password* | Público |
| GET | `/auth/logout` | *Logout* | Autenticado |
| GET | `/docs` | *Swagger UI* | Público |
| GET | `/users/me` | Perfil próprio | Autenticado |
| GET | `/users` | Lista utilizadores | Treinador/Admin |
| GET | `/planos` | Lista planos | Autenticado |
| GET | `/planos/:id` | Obter plano | Autenticado |
| POST | `/planos` | Criar plano  | Autenticado |
| PUT | `/planos/:id` | Atualizar plano  | Autenticado |
| DELETE | `/planos/:id` | Apagar plano  | Autenticado |
| GET | `/exercicios?plano_id=X` | Lista exercícios | Autenticado |
| GET | `/exercicios/:id` | Obter exercício | Autenticado |
| POST | `/exercicios` | Criar exercício  | Autenticado |
| PUT | `/exercicios/:id` | Atualizar exercício  | Autenticado |
| DELETE | `/exercicios/:id` | Apagar exercício  | Autenticado |
| GET | `/sessoes` | Lista sessões | Autenticado |
| GET | `/sessoes/:id` | Obter sessão | Autenticado |
| POST | `/sessoes` | Criar sessão | Treinador/Admin |
| PUT | `/sessoes/:id` | Atualizar sessão | Autenticado |
| DELETE | `/sessoes/:id` | Apagar sessão | Treinador/Admin |
| GET | `/avaliacoes` | Lista avaliações físicas | Autenticado |
| GET | `/avaliacoes/:id` | Obter avaliação | Autenticado |
| POST | `/avaliacoes` | Criar avaliação  | Autenticado |
| PUT | `/avaliacoes/:id` | Atualizar avaliação  | Autenticado |
| DELETE | `/avaliacoes/:id` | Apagar avaliação  | Autenticado |
| GET | `/metas` | Lista metas | Autenticado |
| GET | `/metas/:id` | Obter meta | Autenticado |
| POST | `/metas` | Criar meta  | Autenticado |
| PUT | `/metas/:id` | Atualizar meta  | Autenticado |
| DELETE | `/metas/:id` | Apagar meta ( | Autenticado |




## Detalhes de implementação 
### Requisitos Obrigatórios


**Utilização de, pelo menos, 4 verbos  do protocolo *HTTP* - *CRUD*:**

Todos os métodos *HTTP* (*GET*, *POST*, *PUT*, *DELETE*) foram implementados em cada recurso. Exemplo de implementação:

**GET - listar planos**

```javascript
// src/controllers/planosController.js

async function listar(req, res) {
  try {
    const clienteIdQuery = req.query.cliente_id;
    let where = {};

    if (req.user.role === 'cliente') {
      where = { clienteId: req.user.id };
    } else if (req.user.role === 'treinador') {
      // Sem filtros, treinador só vê os planos em que é responsável.
      where = { treinadorId: req.user.id };
    }

    if (clienteIdQuery !== undefined && clienteIdQuery !== null && String(clienteIdQuery).trim() !== '') {
      if (req.user.role === 'cliente') {
        return res.status(403).json({ erro: 'Filtro cliente_id só pode ser usado por admin ou treinador.' });
      }

      const clienteIdFiltro = Number(clienteIdQuery);
      if (!Number.isInteger(clienteIdFiltro) || clienteIdFiltro <= 0) {
        return res.status(400).json({ erro: 'cliente_id inválido.' });
      }

      if (req.user.role === 'treinador') {
        // Com cliente_id, treinador só pode ver planos profissionais dele nesse cliente.
        const planosProfissionaisCliente = await PlanoTreino.findAll({
          where: { clienteId: clienteIdFiltro, tipo: 'profissional' },
          attributes: ['treinadorId']
        });
        const clienteTemPlanoProfissional = planosProfissionaisCliente.length > 0;
        const clienteAssociadoAoTreinador = planosProfissionaisCliente.some((p) => p.treinadorId === req.user.id);

        if (!clienteAssociadoAoTreinador && clienteTemPlanoProfissional) {
          return res.status(403).json({ erro: 'Filtro cliente_id não permitido para este cliente.' });
        }

        where = { clienteId: clienteIdFiltro, tipo: 'profissional', treinadorId: req.user.id };
      } else {
        where = { ...where, clienteId: clienteIdFiltro };
      }
    }

    const planos = await PlanoTreino.findAll({
      where,
      include: [
        { model: Utilizador, as: 'treinador', attributes: atribsUtilizador },
        { model: Utilizador, as: 'cliente',   attributes: atribsUtilizador }
      ],
      order: [['id', 'ASC']]
    });

    return res.status(200).json(planos);
  } catch (err) {
    console.error(err);
    return res.status(500).json({ erro: 'Erro interno do servidor.' });
  }
}
```

**POST - criar plano**

```javascript
// src/controllers/planosController.js

async function criar(req, res) {
  const { titulo, descricao, objetivo, duracaoSem } = req.body;
  const clienteIdPayload = req.body.clienteId ?? req.body.cliente_id;
  const treinadorIdPayload = req.body.treinadorId ?? req.body.treinador_id;

  if (!titulo || !objetivo || !duracaoSem) {
    return res.status(400).json({ erro: 'titulo, objetivo e duracaoSem são obrigatórios.' });
  }

  if (!OBJETIVOS_VALIDOS.includes(objetivo)) {
    return res.status(400).json({ erro: `objetivo deve ser um de: ${OBJETIVOS_VALIDOS.join(', ')}` });
  }

  try {
    let tipo, treinadorId, clienteIdFinal;

    if (req.user.role === 'cliente') {
      // Cliente só pode criar plano pessoal para si.

      if (clienteIdPayload !== undefined && clienteIdPayload !== null && Number(clienteIdPayload) !== req.user.id) {
        return res.status(403).json({ erro: 'clienteId inválido. Cliente só pode criar plano para si próprio.' });
      }
      if (treinadorIdPayload !== undefined && treinadorIdPayload !== null) {
        return res.status(403).json({ erro: 'Cliente não pode associar treinador ao plano pessoal.' });
      }
      tipo          = 'pessoal';
      treinadorId   = null;
      clienteIdFinal = req.user.id;
    } else {
      // Treinador/admin criam plano profissional para um cliente.

      if (!clienteIdPayload) {
        return res.status(400).json({ erro: 'clienteId é obrigatório para treinadores.' });
      }
      const cliente = await Utilizador.findByPk(clienteIdPayload);
      if (!cliente || cliente.role !== 'cliente') {
        return res.status(400).json({ erro: 'clienteId inválido. Deve referenciar um utilizador com role cliente.' });
      }
      if (req.user.role === 'treinador' && Number(clienteIdPayload) === req.user.id) {
        return res.status(400).json({ erro: 'Treinador não pode criar plano para si próprio.' });
      }
      if (req.user.role === 'treinador' && treinadorIdPayload !== undefined && Number(treinadorIdPayload) !== req.user.id) {
        return res.status(403).json({ erro: 'Treinador não pode associar outro treinador ao plano.' });
      }
      if (req.user.role === 'treinador') {
        const planoOutroTreinador = await PlanoTreino.findOne({
          where: {
            clienteId: Number(clienteIdPayload),
            tipo: 'profissional',
            treinadorId: { [Op.ne]: req.user.id }
          }
        });
        if (planoOutroTreinador) {
          return res.status(403).json({ erro: 'Cliente já está associado a outro treinador.' });
        }
      }
      tipo          = 'profissional';
      treinadorId   = req.user.id;
      clienteIdFinal = clienteIdPayload;
    }

    const plano = await PlanoTreino.create({
      titulo,
      descricao: descricao || null,
      objetivo,
      duracaoSem,
      tipo,
      treinadorId,
      clienteId: clienteIdFinal
    });

    return res.status(201).json({ mensagem: 'Plano criado com sucesso.', id: plano.id });
  } catch (err) {
    console.error(err);
    return res.status(500).json({ erro: 'Erro interno do servidor.' });
  }
}
```

**PUT - atualizar plano**

```javascript
// src/controllers/planosController.js

async function atualizar(req, res) {
  try {
    const plano = await PlanoTreino.findByPk(req.params.id);
    if (!plano) return res.status(404).json({ erro: 'Plano não encontrado.' });

    if (req.user.role === 'treinador' && plano.treinadorId !== req.user.id) {
      return res.status(403).json({ erro: 'Não pode editar planos de outro treinador.' });
    }
    if (req.user.role === 'treinador' && plano.tipo !== 'profissional') {
      return res.status(403).json({ erro: 'Treinador não pode editar planos pessoais.' });
    }
    if (req.user.role === 'cliente' && plano.clienteId !== req.user.id) {
      return res.status(403).json({ erro: 'Não pode editar planos de outro cliente.' });
    }
    if (req.user.role === 'cliente' && plano.tipo === 'profissional') {
      return res.status(403).json({ erro: 'Cliente não pode editar planos do tipo profissional.' });
    }

    const { titulo, descricao, objetivo, duracaoSem, ativo } = req.body;
    const tentouAlterarCamposProtegidos =
      req.body.treinadorId !== undefined ||
      req.body.clienteId !== undefined ||
      req.body.tipo !== undefined;

    if (tentouAlterarCamposProtegidos && req.user.role !== 'admin') {
      return res.status(403).json({ erro: 'Não pode alterar treinadorId, clienteId ou tipo do plano.' });
    }

    if (objetivo && !OBJETIVOS_VALIDOS.includes(objetivo)) {
      return res.status(400).json({ erro: `objetivo deve ser um de: ${OBJETIVOS_VALIDOS.join(', ')}` });
    }

    await plano.update({
      titulo:     titulo     ?? plano.titulo,
      descricao:  descricao  ?? plano.descricao,
      objetivo:   objetivo   ?? plano.objetivo,
      duracaoSem: duracaoSem ?? plano.duracaoSem,
      ativo:      ativo      ?? plano.ativo
    });

    return res.status(200).json({ mensagem: 'Plano atualizado com sucesso.' });
  } catch (err) {
    console.error(err);
    return res.status(500).json({ erro: 'Erro interno do servidor.' });
  }
}
```

**DELETE - apagar plano**

```javascript
// src/controllers/planosController.js

async function apagar(req, res) {
  try {
    const plano = await PlanoTreino.findByPk(req.params.id);
    if (!plano) return res.status(404).json({ erro: 'Plano não encontrado.' });

    if (req.user.role === 'treinador' && plano.treinadorId !== req.user.id) {
      return res.status(403).json({ erro: 'Não pode apagar planos de outro treinador.' });
    }
    if (req.user.role === 'treinador' && plano.tipo !== 'profissional') {
      return res.status(403).json({ erro: 'Treinador não pode apagar planos pessoais.' });
    }
    if (req.user.role === 'cliente' && plano.clienteId !== req.user.id) {
      return res.status(403).json({ erro: 'Não pode apagar planos de outro cliente.' });
    }
    if (req.user.role === 'cliente' && plano.tipo !== 'pessoal') {
      return res.status(403).json({ erro: 'Cliente só pode apagar planos pessoais próprios.' });
    }

    await plano.destroy();
    return res.status(200).json({ mensagem: 'Plano apagado com sucesso.' });
  } catch (err) {
    console.error(err);
    return res.status(500).json({ erro: 'Erro interno do servidor.' });
  }
}
```


**Utilização de pelo menos uma relação de cardinalidade 1:n entre dois dos recursos:**

O projeto implementa várias relações 1:N usando o *Sequelize Associations*. Exemplo de código:

**Relação PlanoTreino → Exercícios (`models/index.js`):**
```javascript
// models/index.js
// 1 Plano → N Exercícios (relação 1:N principal do enunciado)
PlanoTreino.hasMany(Exercicio, {
  foreignKey: 'planoId',
  as: 'exercicios',
  onDelete: 'CASCADE'
});
Exercicio.belongsTo(PlanoTreino, { foreignKey: 'planoId', as: 'plano' });
```

**Relações implementadas:**
- `utilizadores → planos_treino`: Um treinador cria muitos planos; um cliente tem muitos planos atribuídos
- `planos_treino → exercicios`: Um plano tem muitos exercícios *(relação 1:N principal)*
- `planos_treino → sessoes`: Um plano tem muitas sessões registadas
- `utilizadores → sessoes`: Um cliente realiza muitas sessões; um treinador supervisiona muitas sessões
- `utilizadores → avaliacoes_fisicas`: Um cliente tem muitas avaliações físicas
- `utilizadores → metas`: Um cliente tem muitas metas definidas
- `planos_treino → metas`: Um plano pode ter muitas metas associadas

**Disponibilização das representações do estado dos recursos em *JSON*:**

Todos os recursos retornam dados em formato *JSON*. Exemplo de resposta:

```json
{
  "id": 1,
  "titulo": "Plano de Hipertrofia - Nível Intermédio",
  "descricao": "Plano focado no aumento de massa muscular com 4 dias de treino semanais",
  "objetivo": "hipertrofia",
  "duracaoSem": 12,
  "tipo": "profissional",
  "ativo": true,
  "treinador": {
    "id": 2,
    "username": "joao_fitness",
    "email": "joao@gym.pt",
    "role": "treinador"
  },
  "cliente": {
    "id": 5,
    "username": "pedro_cliente",
    "email": "pedro@gym.pt",
    "role": "cliente"
  },
  "createdAt": "2026-01-01T10:00:00.000Z"
}
```
**Autenticação OAuth2 (GitHub/Google):**

O projeto implementa autenticação OAuth2 com provedores externos (`/auth/github`, `/auth/google`, usando `Passport.js` para criar sessão e proteger os recursos.

```javascript
// src/routes/auth.js
router.get('/github',
  passport.authenticate('github', { scope: ['user:email'] })
);

router.get('/github/callback',
  passport.authenticate('github', { failureRedirect: '/login' }),
  (req, res) => res.redirect('/')
);

router.get('/google',
  passport.authenticate('google', { scope: ['profile', 'email'] })
);

router.get('/google/callback',
  passport.authenticate('google', { failureRedirect: '/login' }),
  (req, res) => res.redirect('/')
);
```

**Apresentação de Logs de Autenticação na Consola**

A cada pedido autenticado (`ensureAnyAuth`):
```
┌─────────────────────────────────────────────
│ Utilizador Autenticado
│  ID       : 2
│  Username : joao_fitness
│  Nome     : João Ferreira
│  Email    : joao@email.pt
│  Provider : github
│  Role     : treinador
│  Rota     : GET /planos
│  Data/Hora: 2026-04-30T10:30:00.000Z
└─────────────────────────────────────────────
```

### Requisitos Extra

**Filtros através de parâmetros *HTTP*:**

Foi implementado suporte a **filtros** em endpoints de listagem (ex.: `GET /planos?cliente_id=...`, `GET /sessoes?estado=...`, `GET /metas?plano_id=...`). 

**Exemplo (filtro):**

```javascript
// src/controllers/planosController.js
async function listar(req, res) {
  try {
    const clienteIdQuery = req.query.cliente_id;
    let where = {};

    if (req.user.role === 'cliente') {
      where = { clienteId: req.user.id };
    } else if (req.user.role === 'treinador') {
      // Sem filtros, treinador só vê os planos em que é responsável.
      where = { treinadorId: req.user.id };
    }

    if (clienteIdQuery !== undefined && clienteIdQuery !== null && String(clienteIdQuery).trim() !== '') {
      if (req.user.role === 'cliente') {
        return res.status(403).json({ erro: 'Filtro cliente_id só pode ser usado por admin ou treinador.' });
      }

      const clienteIdFiltro = Number(clienteIdQuery);
      if (!Number.isInteger(clienteIdFiltro) || clienteIdFiltro <= 0) {
        return res.status(400).json({ erro: 'cliente_id inválido.' });
      }

      if (req.user.role === 'treinador') {
        // Com cliente_id, treinador só pode ver planos profissionais dele nesse cliente.
        const planosProfissionaisCliente = await PlanoTreino.findAll({
          where: { clienteId: clienteIdFiltro, tipo: 'profissional' },
          attributes: ['treinadorId']
        });
        const clienteTemPlanoProfissional = planosProfissionaisCliente.length > 0;
        const clienteAssociadoAoTreinador = planosProfissionaisCliente.some((p) => p.treinadorId === req.user.id);

        if (!clienteAssociadoAoTreinador && clienteTemPlanoProfissional) {
          return res.status(403).json({ erro: 'Filtro cliente_id não permitido para este cliente.' });
        }

        where = { clienteId: clienteIdFiltro, tipo: 'profissional', treinadorId: req.user.id };
      } else {
        where = { ...where, clienteId: clienteIdFiltro };
      }
    }

    const planos = await PlanoTreino.findAll({
      where,
      include: [
        { model: Utilizador, as: 'treinador', attributes: atribsUtilizador },
        { model: Utilizador, as: 'cliente',   attributes: atribsUtilizador }
      ],
      order: [['id', 'ASC']]
    });

    return res.status(200).json(planos);
  } catch (err) {
    console.error(err);
    return res.status(500).json({ erro: 'Erro interno do servidor.' });
  }
}
```

**Autorização por *roles*:**

A *API* implementa autorização baseada em *roles* (`admin`, `treinador`, `cliente`) através do *middleware* `ensureRole`:

**Middleware de autorização (`src/middleware/auth.js`):**
```javascript
// src/middleware/auth.js
function ensureRole(...roles) {
  return (req, res, next) => {
    if (!req.user || !roles.includes(req.user.role)) {
      return res.status(403).json({
        erro: `Acesso negado. Requer role: ${roles.join(' ou ')}.`
      });
    }
    next();
  };
}
```

**Aplicação nas rotas (`src/routes/sessoes.js`):**
```javascript
// src/routes/sessoes.js
router.get('/',    ensureAnyAuth, ctrl.listar);
router.get('/:id', ensureAnyAuth, ctrl.obter);
router.post('/',   ensureAnyAuth, ensureRole('admin', 'treinador'), ctrl.criar);
router.put('/:id', ensureAnyAuth, ctrl.atualizar);
router.delete('/:id', ensureAnyAuth, ensureRole('admin', 'treinador'), ctrl.apagar);
```

**Autenticação (Basic, API Key, OAuth2)**

A *API* suporta três mecanismos alternativos de autenticação:

- **OAuth2** (*GitHub*/*Google*) - cria sessão (`connect.sid`) via `Passport.js`
- **API Key** - header `X-API-Key`
- **Basic Auth** - *HTTP Basic* no header `Authorization` (valida *username*/*password* com `bcrypt`)

**Exemplos de Pedidos:**

**1. *Login* com *Basic Auth*:**
```http
POST /auth/login HTTP/1.1
Host: localhost:3000
Content-Type: application/json

{
  "usernameOrEmail": "admin",
  "password": "admin123"
}
```

**2. Listar planos de treino (com *API Key*):**
```http
GET /planos HTTP/1.1
Host: localhost:3000
X-API-Key: apikey_admin_001
```

**3. Criar um plano de treino:**
```http
POST /planos HTTP/1.1
Host: localhost:3000
Content-Type: application/json
X-API-Key: apikey_admin_001

{
  "titulo": "Plano de Emagrecimento",
  "objetivo": "emagrecimento",
  "duracaoSem": 8,
  "clienteId": 5
}
```

**4. Listar exercícios de um plano:**
```http
GET /exercicios?plano_id=1 HTTP/1.1
Host: localhost:3000
X-API-Key: apikey_admin_001
```

**5. Registar uma avaliação física:**
```http
POST /avaliacoes HTTP/1.1
Host: localhost:3000
Content-Type: application/json
X-API-Key: apikey_treinador_001

{
  "clienteId": 5,
  "tipo": "profissional",
  "data": "2026-04-15",
  "pesoKg": 78.5,
  "alturaCm": 175,
  "percGordura": 18.5
}
```
---

[^ Início](../README.md) | [Próximo >](c2.md)
:--- | ---:
